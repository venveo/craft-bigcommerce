<h2>{{ "BigCommerce Sync"|t('bigcommerce') }}</h2>
<form method="POST">
    {{ actionInput('') }}
    {{ csrfInput() }}
    <a class="btn submit formsubmit" data-icon="refresh"
       data-action="bigcommerce/products/sync"
       data-confirm="{{ 'Are you sure you want run a complete sync of all products?'|t('bigcommerce') }}">
        {{ 'Sync all Products'|t('bigcommerce') }}
    </a>
</form>